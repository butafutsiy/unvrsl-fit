'use strict';
(()=>{
  const W=window,D=document;if(W.__unvrslClientFreeWorkoutV334)return;W.__unvrslClientFreeWorkoutV334=true;
  const E=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const N=v=>{const n=Number(String(v??'').replace(',','.'));return Number.isFinite(n)?n:null};
  const userId=()=>String(W.cloud?.user?.id||'');
  const isClient=()=>!!userId()&&(typeof W.unvrslTrainerMode==='function'?!W.unvrslTrainerMode():W.cloud?.profile?.role!=='trainer');
  const draft=()=>{if(!st.clientFreeWorkoutDraftV334||typeof st.clientFreeWorkoutDraftV334!=='object')st.clientFreeWorkoutDraftV334={};const id=userId();if(!Array.isArray(st.clientFreeWorkoutDraftV334[id]))st.clientFreeWorkoutDraftV334[id]=[];return st.clientFreeWorkoutDraftV334[id]};
  const persist=()=>{try{save()}catch(_){ }};
  function names(){
    try{const records=typeof W.catalogRecords==='function'?W.catalogRecords():[],values=records.map(x=>x.strictName||(x.custom?x.n:(typeof W.ruExerciseName==='function'?W.ruExerciseName(x.n):x.n))).filter(Boolean);return W.UNVRSL_EXERCISE_PICKER_V331?.list(values)||[...new Map(values.map(x=>[String(x).toLocaleLowerCase('ru'),String(x)])).values()].sort((a,b)=>a.localeCompare(b,'ru'))}catch(_){return W.UNVRSL_EXERCISE_PICKER_V331?.list()||[]}
  }
  function rows(){const items=draft();return items.length?items.map((x,i)=>`<article class="cfw-row"><div><b>${E(x.name)}</b><span>${x.sets} × ${x.repMin===x.repMax?x.repMin:`${x.repMin}–${x.repMax}`} · ${x.weight>0?`${String(x.weight).replace('.',',')} кг`:'автовес'} · RPE ${String(x.rpe).replace('.',',')} · отдых ${x.rest} сек</span></div><button type="button" onclick="clientFreeWorkoutRemoveV334(${i})" aria-label="Удалить ${E(x.name)}">×</button></article>`).join(''):'<div class="cfw-empty">Добавь первое упражнение из нашей базы.</div>'}
  function builder(){
    const list=names();W.modal?.(`<div class="sheet-grabber"></div><div class="cfw-head"><div><div class="cfw-kicker">СВОБОДНЫЙ ФОРМАТ</div><h2>Собрать тренировку</h2><p>Упражнения и нагрузку выбираешь сам. Нулевой вес будет рассчитан автоматически.</p></div><button class="btn tiny" onclick="closeModal()">✕</button></div><div class="cfw-form"><label class="cfw-wide"><span>Упражнение</span><input id="cfwExercise" list="cfwExerciseList" placeholder="Начни вводить название" autocomplete="off"><datalist id="cfwExerciseList">${list.map(n=>`<option value="${E(n)}"></option>`).join('')}</datalist></label><label><span>Подходы</span><input id="cfwSets" type="number" min="1" max="12" value="3"></label><label><span>Повторы от</span><input id="cfwRepMin" type="number" min="1" max="100" value="8"></label><label><span>Повторы до</span><input id="cfwRepMax" type="number" min="1" max="100" value="12"></label><label><span>Вес, кг</span><input id="cfwWeight" type="number" inputmode="decimal" min="0" max="999" step="0.5" value="0"></label><label><span>RPE</span><select id="cfwRpe">${[7,7.5,8,8.5,9].map(x=>`<option value="${x}"${x===8?' selected':''}>${String(x).replace('.',',')}</option>`).join('')}</select></label><label><span>Отдых, сек</span><input id="cfwRest" type="number" min="15" max="600" step="15" value="90"></label></div><button class="btn full cfw-add" onclick="clientFreeWorkoutAddV334()">＋ Добавить упражнение</button><div id="cfwRows" class="cfw-rows">${rows()}</div><div class="cfw-actions"><button class="btn" onclick="clientFreeWorkoutClearV334()"${draft().length?'':' disabled'}>Очистить</button><button class="btn primary" onclick="clientFreeWorkoutStartV334()"${draft().length?'':' disabled'}>Начать тренировку</button></div>`)
  }
  function readNumber(id,fallback,min,max){const n=N(D.getElementById(id)?.value);return n==null?fallback:Math.max(min,Math.min(max,n))}
  W.clientFreeWorkoutOpenV334=builder;
  W.clientFreeWorkoutAddV334=function(){
    const raw=String(D.getElementById('cfwExercise')?.value||'').trim(),match=names().find(x=>x.toLocaleLowerCase('ru')===raw.toLocaleLowerCase('ru'));if(!match)return W.toast?.('Выбери упражнение из списка');
    const repMin=Math.round(readNumber('cfwRepMin',8,1,100)),repMax=Math.max(repMin,Math.round(readNumber('cfwRepMax',repMin,1,100)));
    draft().push({id:`cfw-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`,name:match,sets:Math.round(readNumber('cfwSets',3,1,12)),repMin,repMax,weight:readNumber('cfwWeight',0,0,999),rpe:readNumber('cfwRpe',8,5,10),rest:Math.round(readNumber('cfwRest',90,15,600))});persist();const host=D.getElementById('cfwRows');if(host)host.innerHTML=rows();builder()
  };
  W.clientFreeWorkoutRemoveV334=function(index){draft().splice(Number(index),1);persist();builder()};
  W.clientFreeWorkoutClearV334=function(){if(!draft().length||!W.confirm('Очистить список упражнений?'))return;draft().splice(0);persist();builder()};
  function program(){
    const id=`client-free-v334-${userId()}`,items=draft(),ex=items.map(item=>{const meta=typeof W.inferCustomMeta==='function'?W.inferCustomMeta(item.name):{};return{id:item.id,n:item.name,sourceId:null,bp:meta?.bp||'',tg:meta?.tg||'',eq:meta?.eq||'',method:'STANDARD',rpe:item.rpe,tempo:'2-0-2',rest:item.rest,note:'Свободная тренировка',weightMode:item.weight>0?'manual':'auto',programWeightMode:item.weight>0?'prescribed':'autoweight',repMin:item.repMin,repMax:item.repMax,sets:Array.from({length:item.sets},(_,i)=>({label:String(i+1),w:item.weight,r:item.repMin,rMin:item.repMin,rMax:item.repMax,rest:item.rest}))}}),value={id,name:'Свободная тренировка',isClientFreeWorkout:true,ownerUserId:userId(),created:Date.now(),updated:Date.now(),weeks:[{n:1,useIntensity:false,days:[{id:`${id}-day`,name:'Свободная тренировка',ex}]}]};
    if(!Array.isArray(st.programs))st.programs=[];const index=st.programs.findIndex(x=>x?.id===id);if(index>=0)value.created=st.programs[index].created||value.created,st.programs[index]=value;else st.programs.push(value);persist();return value
  }
  W.clientFreeWorkoutStartV334=function(){if(!draft().length)return W.toast?.('Сначала добавь упражнения');const p=program();if(typeof W.beginProgramDay!=='function'&&typeof beginProgramDay!=='function')return W.toast?.('Экран тренировки ещё загружается');(W.beginProgramDay||beginProgramDay)(p.id,0,0)};
  function inject(){
    const root=D.getElementById('plan');if(!root||!isClient()||root.querySelector('[data-client-free-v334]'))return;const card=D.createElement('div');card.dataset.clientFreeV334='1';card.innerHTML=`<div class="section">СВОБОДНАЯ ТРЕНИРОВКА</div><button type="button" class="card cfw-plan-card" onclick="clientFreeWorkoutOpenV334()"><div><span>Свой набор упражнений</span><b>Свободная тренировка</b><small>${draft().length?`${draft().length} упражнений уже выбрано`:'Собери тренировку из нашей базы'}</small></div><strong>Собрать ›</strong></button>`;const profile=root.querySelector('#clientPlanProfileV255');if(profile)profile.after(card);else root.prepend(card)
  }
  const style=D.createElement('style');style.id='client-free-workout-v334-style';style.textContent=`.cfw-plan-card{display:flex;width:100%;align-items:center;justify-content:space-between;gap:12px;text-align:left;background:linear-gradient(145deg,rgba(100,210,255,.09),#1d1d21 55%);border-color:rgba(100,210,255,.28)}.cfw-plan-card span,.cfw-plan-card small{display:block;color:#8e8e95;font-size:11px}.cfw-plan-card b{display:block;margin:5px 0;font-size:19px}.cfw-plan-card strong{color:#8bddff;font-size:13px;white-space:nowrap}.cfw-head{display:flex;justify-content:space-between;gap:12px}.cfw-head h2{margin:6px 0;font-size:27px}.cfw-head p{margin:0;color:#8e8e95;font-size:12px;line-height:1.4}.cfw-kicker{color:#64d2ff;font-size:11px;font-weight:850;letter-spacing:.12em}.cfw-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin-top:16px}.cfw-form label{display:block}.cfw-form span{display:block;margin:0 0 6px 2px;color:#96969e;font-size:11px}.cfw-form input,.cfw-form select{width:100%;height:48px;padding:0 12px;border:1px solid #36363d;border-radius:14px;background:#19191d;color:#f5f5f7;font-size:15px}.cfw-wide{grid-column:1/-1}.cfw-add{margin-top:10px}.cfw-rows{display:grid;gap:7px;margin-top:12px}.cfw-row{display:grid;grid-template-columns:minmax(0,1fr) 36px;gap:8px;align-items:center;padding:11px 12px;border-radius:15px;background:#232328;border:1px solid #313137}.cfw-row b,.cfw-row span{display:block}.cfw-row b{font-size:13px}.cfw-row span{margin-top:4px;color:#8e8e95;font-size:10px;line-height:1.35}.cfw-row button{width:34px;height:34px;border-radius:11px;background:rgba(255,69,58,.1);color:#ff8179;font-size:19px}.cfw-empty{padding:17px;border:1px dashed #37373e;border-radius:15px;color:#85858d;font-size:12px}.cfw-actions{display:grid;grid-template-columns:.7fr 1.3fr;gap:8px;margin-top:13px}.cfw-actions .btn{min-height:50px}@media(max-width:380px){.cfw-form{grid-template-columns:1fr}.cfw-wide{grid-column:auto}.cfw-actions{grid-template-columns:1fr}}`;D.head.appendChild(style);
  function wrap(){const base=W.clientCleanPlanPage;if(typeof base!=='function'||base.__freeWorkoutV334)return;const wrapped=function(){const result=base.apply(this,arguments);queueMicrotask(inject);return result};wrapped.__freeWorkoutV334=true;W.clientCleanPlanPage=wrapped;try{clientCleanPlanPage=wrapped}catch(_){ }}
  [0,200,700,1800,3500].forEach(ms=>setTimeout(()=>{wrap();inject()},ms));W.addEventListener('unvrsl:deferred-modules-ready',()=>{wrap();inject()},{passive:true});
})();
