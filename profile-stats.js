'use strict';
// Legacy statistics/profile renderer retired in v249.
// Kept as a no-op file so stale loaders cannot re-install the old statsPage wrapper.
window.__unvrslLegacyProfileStatsDisabled=true;
