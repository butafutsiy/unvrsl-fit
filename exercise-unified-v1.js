'use strict';
(()=>{
 function boot(){
  if(window.__unvrslExerciseUnifiedV1)return;window.__unvrslExerciseUnifiedV1=true;
  let limit=180;const PAGE=180;
  const fav=e=>typeof isFavorite==='function'&&isFavorite(String(e.rawId||e.id||e.n||''));
  const recent=e=>Array.isArray(st?.recentExercises)&&st.recentExercises.includes(String(e.rawId||e.id||e.n||''));
  const eqGroup=e=>{try{return typeof equipmentGroup==='function'?equipmentGroup(e):String(e.eq||'')}catch(_){return String(e.eq||'')}};
  const sourceRecords=()=>typeof window.UNVRSL_CATALOG_RECORDS==='function'?window.UNVRSL_CATALOG_RECORDS():[];
  function title(e){if(typeof window.UNVRSL_CATALOG_TITLE==='function'){const t=String(window.UNVRSL_CATALOG_TITLE(e)||'').trim();if(t)return t}try{return ruExerciseName(e.n||e.name||'')}catch(_){return String(e.n||e.name||'Упражнение')}}
  function filtered(){const q=String(typeof exQuery==='undefined'?'':exQuery).trim().toLowerCase();return sourceRecords().filter(e=>{if(typeof exBody!=='undefined'){if(exBody==='favorites'&&!fav(e))return false;if(exBody==='recent'&&!recent(e))return false;if(exBody==='frequent')return false;if(!['all','favorites','recent','frequent'].includes(exBody)&&e.bp!==exBody)return false}if(typeof exEquipment!=='undefined'&&exEquipment!=='all'&&eqGroup(e)!==exEquipment)return false;const h=`${title(e)} ${e.n||''} ${(BP_RU&&BP_RU[e.bp])||''} ${(EQ_RU&&EQ_RU[e.eq])||''} ${typeof ruTarget==='function'?ruTarget(e.tg):e.tg||''}`.toLowerCase();return !q||h.includes(q)})}
  function row(e){const t=title(e),body=(BP_RU&&BP_RU[e.bp])||e.bp||'—',eq=(EQ_RU&&EQ_RU[e.eq])||e.eq||'—',tg=typeof ruTarget==='function'?ruTarget(e.tg):e.tg||'—',gif=e.gif||e.gif_url||'',id=String(e.id).startsWith('og:')?e.id:`og:${e.id}`;return `<div class="card exlib exlib-btn smart-ex-row" onclick="openExerciseDetail('${encodeURIComponent(id)}')"><div class="exercise-list-row"><img class="ex-thumb" src="${mediaUrl(gif)}" loading="lazy" alt="${esc(t)}"><div class="grow"><b>${esc(t)}</b><div class="catalog-meta">${esc(body)} · ${esc(eq)} · ${esc(tg)}</div></div><span class="chev">›</span></div></div>`}
  function removeModeTabs(){const root=document.querySelector('#exercises');if(!root)return;[...root.querySelectorAll('button')].forEach(b=>{const t=(b.textContent||'').trim();if(t==='Силовые'||t==='Кардио'){const p=b.parentElement;if(p&&[...p.querySelectorAll('button')].some(x=>(x.textContent||'').trim()==='Силовые')&&[...p.querySelectorAll('button')].some(x=>(x.textContent||'').trim()==='Кардио'))p.remove()}})}
  function render(){removeModeTabs();const el=document.querySelector('#exList');if(!el)return;const all=sourceRecords(),f=filtered(),shown=f.slice(0,limit);el.innerHTML=shown.map(row).join('')+(shown.length<f.length?`<button class="btn full" style="margin:12px 0 4px" onclick="showMoreUnifiedExercises()">Показать ещё · ${shown.length} из ${f.length}</button>`:'')+(!f.length?'<div class="card muted">По этому фильтру ничего не найдено.</div>':'');const c=document.querySelector('#catalogCount');if(c)c.textContent=`База упражнений · ${all.length}${f.length!==all.length?` · найдено ${f.length}`:''}`}
  const oldPage=window.exercisesPage;window.exercisesPage=function(){if(typeof oldPage==='function')oldPage.apply(this,arguments);removeModeTabs();render()};try{exercisesPage=window.exercisesPage}catch(_){}
  window.renderExerciseResults=render;try{renderExerciseResults=render}catch(_){};window.showMoreUnifiedExercises=()=>{limit+=PAGE;render()};
  const obs=new MutationObserver(()=>{if(document.querySelector('#exercises.page.active'))removeModeTabs()});obs.observe(document.documentElement,{childList:true,subtree:true});setTimeout(()=>{if(document.querySelector('#exercises.page.active'))render()},100);
 }
 function load(src){return new Promise((ok,bad)=>{if(src.endsWith('exercise-catalog-curation-v4.js')&&window.__unvrslCatalogCurationV4)return ok();if(src.endsWith('exercise-format-v5.js')&&window.UNVRSL_FORMAT_V5_READY)return ok();const s=document.createElement('script');s.src=src;s.onload=ok;s.onerror=bad;document.head.appendChild(s)})}
 if(window.UNVRSL_FORMAT_V5_READY)return boot();
 load('exercise-catalog-curation-v4.js').catch(()=>{}).then(()=>load('exercise-format-v5.js')).then(boot).catch(()=>boot());
})();