'use strict';
(()=>{
 if(window.__unvrslTrainingEngineV197)return;window.__unvrslTrainingEngineV197=true;
 const W=window,N=v=>{if(v===''||v==null)return null;const n=Number(String(v).replace(',','.'));return Number.isFinite(n)?n:null},num=v=>N(v)??0,mean=a=>{a=(a||[]).filter(Number.isFinite);return a.length?a.reduce((s,x)=>s+x,0)/a.length:null},clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
 const base=n=>{try{return W.baseExerciseName?W.baseExerciseName(n):String(n||'').replace(/\s+—\s+.*$/,'').trim()}catch(_){return String(n||'')}};
 const same=(e,n,id)=>(id&&String(e?.sourceId||'')===String(id))||base(e?.n).toLowerCase()===base(n).toLowerCase();
 const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 const style=document.createElement('style');style.id='training-engine-v197-style';style.textContent='.te197-list{display:grid;gap:8px;margin:14px 0}.te197-row{background:#202023;border:1px solid #303034;border-radius:15px;padding:11px 12px;display:flex;justify-content:space-between;gap:12px}.te197-row span{display:block;color:#8e8e93;font-size:12px;margin-top:3px}.te197-ready{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}.te197-item{background:#202023;border:1px solid #303034;border-radius:18px;padding:13px}.te197-item b{display:flex;justify-content:space-between;margin-bottom:9px}.te197-item input{width:100%;accent-color:var(--green)}';document.head.appendChild(style);
 if(!Array.isArray(W.st?.readinessLog))W.st.readinessLog=[];
 function program(cur){try{if(cur?.programId&&W.programById){const p=W.programById(cur.programId);if(p)return p}}catch(_){}return(W.st?.programs||[]).find(p=>String(p?.cloudPlanId||'')===String(cur?.planId||''))||null}
 function source(ex,cur){const p=program(cur),week=p?.weeks?.[Math.max(0,(N(cur?.w)||1)-1)];if(!week)return null;let days=week.days||[];const exact=days.find(d=>String(d?.name||'')===String(cur?.c||''));if(exact)days=[exact,...days.filter(d=>d!==exact)];for(const d of days){const s=(d.ex||[]).find(x=>same({n:x.n,sourceId:x.sourceId},ex.n,ex.sourceId));if(s)return s}return null}
 function mode(ex,cur){const s=source(ex,cur);if(s)return(s.sets||[]).some(x=>num(x?.w)>0)?'prescribed':'adaptive';return ex.programWeightMode==='adaptive'?'adaptive':((ex.set||[]).some(x=>num(x?.w)>0)?'prescribed':'adaptive')}
 function occurrenceIndex(ex,cur){const arr=(cur.ex||[]).filter(x=>same(x,ex.n,ex.sourceId));const i=arr.indexOf(ex);return i<0?0:i}
 function programWeight(ex,setIndex,cur){const s=source(ex,cur);if(!s)return num(ex.set?.[setIndex]?.programW??ex.set?.[setIndex]?.w);const sets=s.sets||[];if((ex.set||[]).length>1)return num(sets[setIndex]?.w);return num(sets[occurrenceIndex(ex,cur)]?.w??sets[0]?.w)}
 function effort(x){let rpe=N(x?.rpe),rir=N(x?.rir);if(rir==null&&rpe!=null)rir=10-rpe;if(rpe==null&&rir!=null)rpe=10-rir;return rpe==null?null:{rpe,rir:clamp(rir,0,10)}}
 function rowsFrom(s,ex){const out=[];(s?.ex||[]).forEach(e=>{if(!same(e,ex.n,ex.sourceId))return;(e.set||[]).forEach(x=>{const ef=effort(x),w=N(x.w),r=N(x.r);if(x?.ok&&w>0&&r>0&&ef)out.push({w,r,rir:ef.rir,date:s.date||''})})});return out}
 function localHistory(ex,cur){const ss=W.st?.sessions||[];for(let i=ss.length-1;i>=0;i--){if(String(ss[i]?.id||'')===String(cur.id||''))continue;const r=rowsFrom(ss[i],ex);if(r.length)return r}return[]}
 async function history(ex,cur){const local=localHistory(ex,cur);if(local.length)return local;try{if(!W.cloud?.client||!W.cloud?.user?.id)return[];const q=await W.cloud.client.from('workouts').select('payload,workout_date').eq('user_id',W.cloud.user.id).order('workout_date',{ascending:false}).limit(40);if(q.error)return[];for(const row of q.data||[]){const p=row.payload||{};if(String(p.id||'')===String(cur.id||''))continue;const r=rowsFrom(p,ex);if(r.length)return r}}catch(e){console.warn('v197 history',e)}return[]}
 function e1rm(rows){let a=(rows||[]).map(x=>x.w*(1+(x.r+x.rir)/30)).filter(x=>x>0);if(!a.length)return null;const m=mean(a);if(a.length>=3){const k=a.filter(x=>x>=m*.82&&x<=m*1.18);if(k.length>=2)a=k}return mean(a)}
 function step(ex,rows=[]){let s=2.5;try{s=Number(W.loadStepFor?.(base(ex.n),ex.sourceId||null))||s}catch(_){}const m=mean(rows.map(x=>x.w))||0;if(m<=6)s=Math.min(s,.5);else if(m<=12)s=Math.min(s,1);else if(m<=22)s=Math.min(s,2);return s}
 function round(v,s){return Math.max(s,Math.round(v/s)*s)}
 function target(ex,set,cur){return [N(set?.targetRpe),N(ex?.targetRpe),N(ex?.rpeTarget),N(ex?.target),N(ex?.rpe),N(cur?.target),8].find(x=>x!=null&&x>0)||8}
 async function prepare(cur){
   if(!cur||cur.trainingEngineVersion===197)return;
   delete cur.engine196Prepared;delete cur.engine196FlowShown;delete cur.engine196ReadinessAsked;delete cur.prescribedWeightChoice;delete cur.weightsPrepared194;
   for(const ex of cur.ex||[]){if(ex?.mode==='cardio')continue;ex.programWeightMode=mode(ex,cur);delete ex.recommendation194;delete ex.engine196Recommendation;delete ex.progression187;const rows=await history(ex,cur),cap=e1rm(rows),stp=step(ex,rows);(ex.set||[]).forEach((set,i)=>{set.programW=ex.programWeightMode==='prescribed'?programWeight(ex,i,cur):0;delete set.recommendedW;if(cap>0&&num(set.r)>0)set.recommendedW=round(cap/(1+(num(set.r)+clamp(10-target(ex,set,cur),0,10))/30),stp)});
     if(ex.programWeightMode==='adaptive'){if(cap>0){(ex.set||[]).forEach(s=>{if(!s.ok&&!s.manualOverride&&num(s.recommendedW)>0){s.w=num(s.recommendedW);s.plannedW=s.w;s.baselineW=s.w}});ex.weightDecision='adaptive_auto'}else ex.weightDecision='calibration'}
     else{(ex.set||[]).forEach(s=>{if(!s.ok&&!s.manualOverride){s.w=num(s.programW);s.plannedW=s.w;s.baselineW=s.w}});ex.weightDecision=cap>0&&(ex.set||[]).some(s=>num(s.recommendedW)>0)?'awaiting_choice':'program_no_history'}
   }
   cur.trainingEngineVersion=197;cur.trainingWeightChoice=null;cur.trainingReadinessDone=false;try{W.save?.();W.startPage?.()}catch(_){}}
 function awaiting(cur){return(cur?.ex||[]).filter(ex=>ex.programWeightMode==='prescribed'&&ex.weightDecision==='awaiting_choice')}
 function restore(cur){let ch=false;awaiting(cur).forEach(ex=>(ex.set||[]).forEach(s=>{if(s.ok||s.manualOverride)return;const w=num(s.programW);if(w>0&&num(s.w)!==w){s.w=w;s.plannedW=w;s.baselineW=w;ch=true}}));if(ch)try{W.save?.();W.startPage?.()}catch(_){}return ch}
 function showChoice(cur){const list=awaiting(cur);if(!list.length)return showReadiness(cur);const groups=new Map();list.forEach(ex=>{const k=ex.sourceId?`id:${ex.sourceId}`:`n:${base(ex.n).toLowerCase()}`;if(!groups.has(k))groups.set(k,{name:base(ex.n),plan:[],rec:[]});const g=groups.get(k);(ex.set||[]).forEach(s=>{if(num(s.programW)>0)g.plan.push(num(s.programW));if(num(s.recommendedW)>0)g.rec.push(num(s.recommendedW))})});const html=[...groups.values()].map(g=>`<div class="te197-row"><div><b>${esc(g.name)}</b><span>План: ${g.plan.join(' / ')} кг</span></div><div style="text-align:right"><b>${g.rec.join(' / ')} кг</b><span>рекомендация</span></div></div>`).join('');W.modal?.(`<div class="sheet-grabber"></div><h2>Рабочие веса</h2><div class="muted">Вес программы не меняется автоматически. Выбери, использовать рекомендации по прошлой тренировке или оставить план.</div><div class="te197-list">${html}</div><button class="btn primary full" onclick="trainingChooseWeights197('recommendation')">Применить рекомендации</button><button class="btn full" style="margin-top:10px" onclick="trainingChooseWeights197('program')">Оставить веса программы</button>`)}
 function choose(kind){const cur=W.st?.current;if(!cur)return;awaiting(cur).forEach(ex=>{(ex.set||[]).forEach(s=>{if(s.ok||s.manualOverride)return;const w=kind==='recommendation'?num(s.recommendedW):num(s.programW);if(w>0){s.w=w;s.plannedW=w;s.baselineW=w}});ex.weightDecision=kind});cur.trainingWeightChoice=kind;try{W.save?.();W.startPage?.();W.closeModal?.()}catch(_){}setTimeout(()=>showReadiness(cur),20)}
 function readiness(){const g=id=>num(document.getElementById(id)?.value||3),sl=g('te197Sleep'),en=g('te197Energy'),so=g('te197Sore'),st=g('te197Stress'),pos=v=>(v-1)/4,neg=v=>(5-v)/4,score=Math.round(pos(sl)*25+pos(en)*30+neg(so)*30+neg(st)*15);let factor=1,volume=1,drop=0;if(score<20){factor=.90;volume=.70}else if(score<35){factor=.925;drop=1}else if(score<50)factor=.95;else if(score>=92)factor=1.025;return{sleep:sl,energy:en,soreness:so,stress:st,score,factor,volume,drop,at:new Date().toISOString()}}
 function ritem(id,label){return `<div class="te197-item"><b><span>${label}</span><span id="${id}V">3</span></b><input id="${id}" type="range" min="1" max="5" value="3" oninput="document.getElementById('${id}V').textContent=this.value"></div>`}
 function showReadiness(cur){if(cur.trainingReadinessDone)return;W.modal?.(`<div class="sheet-grabber"></div><h2>Готовность к тренировке</h2><div class="muted">Вес уже определён. Чек-ин только корректирует его по состоянию.</div><div class="te197-ready">${ritem('te197Sleep','Сон')}${ritem('te197Energy','Энергия')}${ritem('te197Sore','Крепатура')}${ritem('te197Stress','Стресс')}</div><button class="btn primary full" onclick="trainingConfirmReadiness197(true)">Скорректировать по самочувствию</button><button class="btn full" style="margin-top:10px" onclick="trainingConfirmReadiness197(false)">Оставить выбранные веса</button>`)}
 function confirm(adjust){const cur=W.st?.current;if(!cur)return;const d=readiness();(cur.ex||[]).forEach(ex=>{const stp=step(ex);(ex.set||[]).forEach(s=>{if(s.ok||s.manualOverride||num(s.plannedW)<=0)return;s.w=adjust?round(num(s.plannedW)*d.factor,stp):num(s.plannedW)});if(adjust&&d.factor<1){let keep=ex.set?.length||0;if(d.volume<1)keep=Math.max(1,Math.ceil(keep*d.volume));else if(d.drop)keep=Math.max(1,keep-d.drop);if(keep<(ex.set?.length||0))ex.set=ex.set.slice(0,keep)}});cur.readiness=d;cur.trainingReadinessDone=true;W.st.readinessLog.push({date:cur.date,sessionId:cur.id,...d});try{W.save?.();W.startPage?.();W.closeModal?.()}catch(_){}}
 let last='',busy=false;
 async function tick(){const cur=W.st?.current;if(!cur?.id||cur.ended)return;const id=String(cur.id);if(id!==last){last=id;busy=false}if(busy)return;busy=true;try{if(cur.trainingEngineVersion!==197)await prepare(cur);restore(cur);if(awaiting(cur).length&&!cur.trainingWeightChoice)showChoice(cur);else if(!cur.trainingReadinessDone)showReadiness(cur);document.querySelectorAll('#start .smart-suggest').forEach(x=>x.style.display='none')}finally{busy=false}}
 W.trainingChooseWeights197=choose;W.trainingConfirmReadiness197=confirm;W.trainingEngine197Tick=tick;
 setInterval(tick,350);[0,100,400,1000,2000].forEach(t=>setTimeout(tick,t));
})();